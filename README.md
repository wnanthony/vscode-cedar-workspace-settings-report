# Cedar Workspace Settings Report

A VS Code extension that creates a portable Markdown audit of `.vscode/settings.json`: setting names, workspace values, effective values, override scope, and portability warnings. It never changes settings.

Run `npm test`, `npm run lint`, and `npm run package`. Install the VSIX and invoke **Cedar: Create Workspace Settings Report** from the Command Palette.

